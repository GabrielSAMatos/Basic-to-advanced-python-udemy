lista = ['Maria', 'Helena', 'Luiz']
indices = range(len(lista))

for nome in range (len(lista)):
    print(nome, lista[nome], type(lista[nome]))
print('-'*25)
for indice in indices:
    print(indice, lista[indice], type(lista[indice]))

